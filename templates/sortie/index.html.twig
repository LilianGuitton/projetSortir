{% extends 'base.html.twig' %}

{% block title %}Créer une sortie{% endblock %}

{% block stylesheets %}
    <link href='https://fonts.googleapis.com/css?family=Montserrat:700' rel='stylesheet' type='text/css'>
{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
<div class="example-wrapper">
    <h1 class="titre1">Créer une sortie</h1>

                                            <!-- FORM -->
    <div>
        {{ form_start(createForm) }}



                <div>
                    {{ form_row(createForm.nom) }}
                </div>
                <div>
                    {{ form_row(createForm.dateHeureDebut) }}
                </div>
                <div>
                    {{ form_row(createForm.duree) }}
                </div>
                <div>
                    {{ form_row(createForm.dateLimiteInscription) }}
                </div>
                <div>
                    {{ form_row(createForm.nbInscriptionMax) }}
                </div>
                <div>

                </div>
                <div>
                    {{ form_row(createForm.lieu) }}
                </div>

{#
<div>
    <label for="{{ field_name(createForm.nom) }}">
         Nom :
     </label>
    <input type="text" id="{{ field_name(createForm.nom) }}"
        name="{{ field_name(createForm.nom) }}">
    {% for error in field_errors(createForm.nom) %}
        <div class="error">{{ error }}</div>
    {% endfor %}
</div>

<div>
    {{ form_widget(createForm.dateHeureDebut) }}
</div>

<div>
    <label for="{{ field_name(createForm.duree) }}">
        Durée de la sortie en min :
    </label>
    <input type="number" id="{{ field_name(createForm.duree) }}"
        name="{{ field_name(createForm.duree) }}" min="20">
    {% for error in field_errors(createForm.duree) %}
        <div class="error">{{ error }}</div>
    {% endfor %}
</div>

<div>
    {{ form_widget(createForm.dateLimiteInscription) }}

</div>

<div>
    <label for="{{ field_name(createForm.nbInscriptionMax) }}">
        Nombre d'inscription maximum :
    </label>
    <input type="number" id="{{ field_name(createForm.nbInscriptionMax) }}"
        name="{{ field_name(createForm.nbInscriptionMax) }}" min="2">
    {% for error in field_errors(createForm.nbInscriptionMax) %}
        <div class="error">{{ error }}</div>
    {% endfor %}
</div>

<div>

    {{ form_label(createForm.lieu) }}
    {{ form_widget(createForm.lieu) }}

</div>#}

                                                <!-- BUTTON -->
        <div>

            <button  name="save" value="save">Enregistrer</button>
            <button  name="publish" value="publish">Publier la sortie</button>

        </div>

        {{ form_end(createForm) }}

        <div>
            <a class="boton" href="{{ path("app_home") }}" >
                Annuler
            </a>
        </div>


    </div>


</div>
{% endblock %}

{% block javascripts %}
<script src="https://code.jquery.com/jquery-latest.min.js" type="text/javascript"></script>
{% endblock %}
